<?php

class GlsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('gls')->truncate();

		$gls = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('gls')->insert($gls);
	}

}
